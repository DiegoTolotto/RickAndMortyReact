import { React, useState } from "react";

export default function Card({ cards = [] }) {
  return (
    <div className="card">
      {cards.map((itens, index) => (
        
      ))}
    </div>
  )
};